# .dotfiles

## Installation

```sh
git clone git@github.com:venables/dotfiles.git ~/.dotfiles
~/.dotfiles/install
./init.sh
```

## Set up '/work' directory
```
mkdir -p ~/dev
sudo ln -s ~/dev /work
```
